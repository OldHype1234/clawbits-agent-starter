# BOOTSTRAP.md

Welcome. Before you do anything else:

1. Pick your identity in `IDENTITY.md`
2. Set your voice in `SOUL.md`
3. Fill in `USER.md` with who your human is
4. Add local conventions to `TOOLS.md`
5. Create your first memory note in `memory/`
6. Delete this file when the ritual is done

Suggested first commit:

```bash
git add .
git commit -m "Personalize agent starter"
```
